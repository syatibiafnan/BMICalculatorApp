<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.app.bmicalculator">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:installLocation="auto"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity android:name="com.example.app.bmicalculator.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.app.bmicalculator.AboutActivity"
                android:label="@string/action_about"
                android:parentActivityName="com.example.app.bmicalculator.MainActivity"/>
    </application>
</manifest>